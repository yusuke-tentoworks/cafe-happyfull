# 進捗ログ: カフェ はぴふる Webサイト

運用ルール:
- 新しい日付を**上に**追記する（最新が一番上）。
- セッション終了時に必ず更新する。次回セッションはこのログを読んでから着手する。
- 機密情報（パスワード・APIキーの値）は書かない。

## 期日

日付は `YYYY-MM-DD` の絶対日付で書く。期日が変わったらこの欄を書き換え、経緯は下のログに追記する。

| 期日 | 内容 | 状態 |
|---|---|---|
| 2026-06-19 | サイト公開（納期） | 達成 |
| 2026-06-20〜21 | プレオープン | 達成 |
| 2026-07 | グランドオープン | 達成 |

---

## 2026-08-05

### 完了したこと
- サポーター募集セクションの4枚目カードを改称・本掲載化。
  - タイトル「食品の委託販売」→「食品の仕入れ協力」、バッジ「委託販売サポーター」→「まちのつくり手サポーター」。
  - 紹介文をドライブの原稿（Googleドキュメント「まちのつくり手サポーター」）をもとに作成。
    原文は紹介トーンの2文だったため、他3枚と揃うよう募集トーンへ寄せて編集。
  - 「準備中」扱いを解除（破線枠と「※現在準備中です」の注釈を削除）。
    未使用になった `.supporter-card--preparing` を `assets/css/style.css` から削除。
  - 注釈を「※お取り扱いの可否や条件は、商品を拝見したうえでご相談させていただきます。」に差し替え。
- お問い合わせフォームのチェックボックス4番目を
  「食品の仕入れ協力（まちのつくり手サポーター）」に変更（value・ラベル両方）。
- PROJECT_MEMO.md の 6-3 / 6-3-2 / 6-3-3 を上記に合わせて更新。
- ローカルプレビュー（python http.server:8765）でDOM・算出スタイルを検証し、
  4枚のカードのタイトル/バッジ/本文/注釈、破線枠の解除、フォームのvalueがすべて意図通りであることを確認。
- ファビコンを円形に切り抜き、白い四角背景を解消。
  - `assets/images/favicon-32.png` … 円の外側を透過（32×32 RGBA）。
  - `assets/images/apple-touch-icon.png` … 同じ絵柄を地色 `#FCFDF1` で埋めて不透過（180×180 RGB）。
  - 元素材は `assets/images/logo.webp`（231×288）。文字入りロゴの中では最高解像度で、
    かつ既に円形マスク＋透過済みだったため円をそのまま流用（旧 apple-touch-icon の180pxより精細）。
    円のbbox `(0,28)-(230,259)` を232×232の正方形に整えてから縮小。
  - 縮小はプリマルチプライドアルファで実施（透過部の黒が縁ににじむのを防ぐため）。
  - `index.html` の `<link rel="icon">` / `<link rel="apple-touch-icon">` は変更なし（画像の差し替えのみ）。
  - ローカルプレビューで両方200、favicon四隅 `(0,0,0,0)`、
    apple-touch-icon四隅 `(252,253,241,255)` を確認。

### 未完了・次回やること
- microCMSのサポーター紹介を入稿する（ドライブの手書きメモの内容。コード変更ではなくCMS入稿作業）。
  - 仕入先として「立花屋」「どんぐりの家もくせいの花」を追加。
  - サポーター（Akko / Soramino / Kula）の写真差し替え・SNSリンク追加。
- 「まちのつくり手サポーター」の紹介文とカード名称をクライアントに確認してもらう（意訳が入っているため）。
- 公開ブロッカー4件のクライアント確認（2026-08-01時点から継続、未解消）。
- 上記が片付き次第 `/seo-check` 以降のQA工程に進む。

### 決定事項・申し送り
- カード命名は「タイトル＝サービス名／バッジ＝サポーター区分」のルールを維持する方針で、
  新名称「まちのつくり手サポーター」はバッジ側に置いた（クライアント選択）。
- **プレビュー方式の訂正**: 下の2026-07-13の記載にある「testブランチ → GitHub Pages」は現在は使っていない。
  Netlifyのブランチデプロイでプレビューする（GitHub PagesではFunctionsが動かず、CORSを緩める必要が出るため）。
- ブラウザプレビューのスクロールとスクリーンショットが本セッションでは不安定だったため、
  見た目の確認はDOM＋算出スタイルで実施。次回スクロール後の実画面も目視確認しておくとよい。
- ファビコンの絵柄は**文字入りロゴのまま**とする（クライアント判断）。
  32pxでは文字が潰れるため文字なしのシンボル丸ロゴ（`logo-square.webp` のカップ＋家）案も出したが、
  ロゴの一貫性を優先して不採用。細かい文字の可読性は問わない方針。
- apple-touch-icon だけ不透過にしているのは、iOSがホーム画面で透過部分を黒く合成する端末があるため。
  透過に揃えたくなった場合はこの点を承知のうえで変更すること。

---

## 2026-07-13

### 完了したこと
- 旧Antigravityルールから新ルールブック（tentoworks-dev-rules）準拠の運用へ移行
  - CLAUDE.md 新設、ガイドラインのコピー・.aiignore を削除（参照方式へ）
  - .claude/settings.json（permissions.deny）配置、.gitignore 追記

### 未完了・次回やること
- なし（移行完了。通常の保守運用へ）

### 決定事項・申し送り
- デプロイ方式: mainへのマージでNetlifyが自動ビルド・公開（プレビュー: testブランチ → GitHub Pages）
